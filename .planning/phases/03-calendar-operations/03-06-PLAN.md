---
phase: 03-calendar-operations
plan: "06"
type: execute
wave: 1
depends_on: []
files_modified:
  - voice-agent/app/services/appointment.py
  - voice-agent/app/services/email.py
  - voice-agent/app/routers/retell.py
autonomous: true
gap_closure: true
requirements:
  - APPT-01
  - APPT-02
  - APPT-03
  - APPT-04
  - APPT-05
  - APPT-06

must_haves:
  truths:
    - "All 14 test_appointment.py tests pass (including the 4 book_appointment tests that were failing with TypeError)"
    - "All 14 test_retell_calendar.py tests pass (no collection error)"
    - "No debug print statements leak caller PII in production logs"
  artifacts:
    - path: "voice-agent/app/services/appointment.py"
      provides: "book_appointment with tz-aware datetime comparison"
      contains: "datetime.now(timezone.utc)"
    - path: "voice-agent/app/services/email.py"
      provides: "sendgrid import guarded by try/except"
      contains: "try:"
    - path: "voice-agent/app/routers/retell.py"
      provides: "clean webhook handler with no debug prints"
  key_links:
    - from: "voice-agent/app/services/appointment.py"
      to: "datetime.now(timezone.utc)"
      via: "timezone imported from datetime module"
      pattern: "from datetime import.*timezone"
    - from: "voice-agent/app/services/email.py"
      to: "sendgrid"
      via: "try/except ImportError guard"
      pattern: "try:.*from sendgrid"
---

<objective>
Close two regressions introduced by Phase 4 that broke Phase 3 test coverage.

Purpose: Restore all 28 previously-passing tests (14 appointment + 14 retell_calendar) to green so Phase 3 requirement coverage is fully verifiable.
Output: Three minimal targeted file edits — no refactoring, no new logic.
</objective>

<execution_context>
@C:/Users/Russell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Russell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@E:/Antigravity/AgentTeam/.planning/PROJECT.md
@E:/Antigravity/AgentTeam/.planning/ROADMAP.md
@E:/Antigravity/AgentTeam/.planning/STATE.md
@E:/Antigravity/AgentTeam/.planning/phases/03-calendar-operations/03-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix timezone-aware datetime comparison in appointment.py</name>
  <files>voice-agent/app/services/appointment.py</files>
  <action>
    Two targeted changes to this file — nothing else:

    1. On the existing import line at line 12:
       CURRENT:  `from datetime import datetime, timedelta`
       CHANGE TO: `from datetime import datetime, timedelta, timezone`

    2. On line 113 inside book_appointment():
       CURRENT:  `now = datetime.now()`
       CHANGE TO: `now = datetime.now(timezone.utc)`

    That is the complete fix. The comparison `if requested_slot < now` on the next line will now work correctly because both sides are timezone-aware. Do NOT change any other logic, variable names, or structure.

    Context: Phase 4 added a past-date guard that calls `datetime.now()` which returns a naive datetime. All test cases (and real Retell payloads) supply ISO-8601 timestamps with timezone offsets, producing tz-aware datetimes. Python raises TypeError when comparing naive vs aware datetimes.
  </action>
  <verify>
    cd E:/Antigravity/AgentTeam && python -m pytest voice-agent/tests/test_appointment.py -v 2>&1 | tail -20
    All 14 tests must pass. Look for "14 passed" in output.
  </verify>
  <done>pytest voice-agent/tests/test_appointment.py reports 14 passed, 0 failed</done>
</task>

<task type="auto">
  <name>Task 2: Guard sendgrid import in email.py and remove debug prints from retell.py</name>
  <files>
    voice-agent/app/services/email.py
    voice-agent/app/routers/retell.py
  </files>
  <action>
    **Change 1 — email.py (lines 11-12):**

    CURRENT:
    ```python
    from sendgrid import SendGridAPIClient
    from sendgrid.helpers.mail import Mail
    ```

    CHANGE TO:
    ```python
    try:
        from sendgrid import SendGridAPIClient
        from sendgrid.helpers.mail import Mail
        _SENDGRID_AVAILABLE = True
    except ImportError:
        SendGridAPIClient = None  # type: ignore[assignment,misc]
        Mail = None  # type: ignore[assignment,misc]
        _SENDGRID_AVAILABLE = False
    ```

    Then update the `_send()` function to guard actual usage. Find the block inside `_send()` that constructs `Mail` and `SendGridAPIClient`. Wrap it so it bails out early if sendgrid is unavailable:

    CURRENT (inside _send, after the `if not to_address:` guard):
    ```python
    try:
        message = Mail(
    ```

    CHANGE TO (add one guard line before the existing try block):
    ```python
    if not _SENDGRID_AVAILABLE:
        logger.debug("sendgrid package not installed; skipping email to %s", to_address)
        return
    try:
        message = Mail(
    ```

    Do NOT change any other logic in email.py.

    **Change 2 — retell.py (lines 71-72):**

    Remove these two lines entirely from `_args_from_body()`:
    ```python
    print("RETELL_BODY_KEYS:", list(body.keys()), flush=True)
    print("RETELL_BODY:", body, flush=True)
    ```

    These debug prints log full Retell request bodies (caller PII: name, phone, email, address) to stdout on every webhook call. They must be removed.

    Do NOT change any other logic in retell.py.
  </action>
  <verify>
    cd E:/Antigravity/AgentTeam && python -m pytest voice-agent/tests/test_retell_calendar.py -v 2>&1 | tail -25
    All 14 tests must collect and pass. Look for "14 passed" in output. No ModuleNotFoundError in output.

    Also verify the print statements are gone:
    grep -n "RETELL_BODY" voice-agent/app/routers/retell.py
    Should return no matches.
  </verify>
  <done>
    pytest voice-agent/tests/test_retell_calendar.py reports 14 passed, 0 failed.
    grep for RETELL_BODY in retell.py returns no matches.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full regression suite confirmation</name>
  <files></files>
  <action>
    Run both test files together to confirm no cross-test interference and all 28 tests pass:

    cd E:/Antigravity/AgentTeam && python -m pytest voice-agent/tests/test_appointment.py voice-agent/tests/test_retell_calendar.py -v

    If any test fails, diagnose from the error output before marking this task done. Do not proceed to SUMMARY if any test is red.
  </action>
  <verify>
    cd E:/Antigravity/AgentTeam && python -m pytest voice-agent/tests/test_appointment.py voice-agent/tests/test_retell_calendar.py -v 2>&1 | tail -10
    Output must contain "28 passed" (or equivalent count if the test files have different totals — confirm zero failures).
  </verify>
  <done>Combined run shows 0 failures across both test files</done>
</task>

</tasks>

<verification>
After all three tasks complete:

1. `pytest voice-agent/tests/test_appointment.py` — 14 passed
2. `pytest voice-agent/tests/test_retell_calendar.py` — 14 passed
3. `grep -n "RETELL_BODY" voice-agent/app/routers/retell.py` — no output
4. `grep -n "datetime.now()" voice-agent/app/services/appointment.py` — no output (only `datetime.now(timezone.utc)` should be present)
5. `grep -n "from sendgrid" voice-agent/app/services/email.py` — appears inside a `try:` block
</verification>

<success_criteria>
- All 14 test_appointment.py tests pass (regression #1 closed)
- All 14 test_retell_calendar.py tests pass (regression #2 closed)
- No PII-leaking print statements remain in retell.py (warning addressed)
- Zero net-new logic introduced — only the three targeted minimal changes described above
- Phase 3 VERIFICATION.md truths #10 and #14 are now verifiable and passing
</success_criteria>

<output>
After completion, create `.planning/phases/03-calendar-operations/03-06-SUMMARY.md`
</output>
