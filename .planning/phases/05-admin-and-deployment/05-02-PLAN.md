---
phase: 05-admin-and-deployment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - voice-agent/Dockerfile
  - voice-agent/railway.toml
  - voice-agent/.env.example
autonomous: false
requirements:
  - INFRA-01

must_haves:
  truths:
    - "The FastAPI backend is reachable at a public Railway HTTPS URL"
    - "GET /health on the Railway URL returns HTTP 200"
    - "POST /webhook on the Railway URL is reachable from the internet (Retell can call it)"
    - "The Railway app starts without error when all required env vars are set"
  artifacts:
    - path: "voice-agent/Dockerfile"
      provides: "Container image definition for Railway deployment"
      contains: "CMD uvicorn"
    - path: "voice-agent/railway.toml"
      provides: "Railway service configuration"
      contains: "healthcheckPath"
    - path: "voice-agent/.env.example"
      provides: "Template of required environment variables for Railway"
  key_links:
    - from: "voice-agent/Dockerfile"
      to: "app.main"
      via: "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
      pattern: "uvicorn.*app.main"
    - from: "Railway environment"
      to: "app.config.Settings"
      via: "env vars injected at runtime (DATABASE_URL, ENCRYPTION_KEY, etc.)"
      pattern: "DATABASE_URL"

user_setup:
  - service: railway
    why: "Deploy FastAPI backend to public HTTPS"
    env_vars:
      - name: DATABASE_URL
        source: "Railway PostgreSQL plugin → Connect → copy DATABASE_URL (use postgresql+asyncpg:// scheme)"
      - name: ENCRYPTION_KEY
        source: "Generate once: python -c \"from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())\""
      - name: OPENAI_API_KEY
        source: "platform.openai.com → API keys"
      - name: ANTHROPIC_API_KEY
        source: "console.anthropic.com → API keys"
      - name: GOOGLE_CLIENT_ID
        source: "Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client ID"
      - name: GOOGLE_CLIENT_SECRET
        source: "Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client ID"
      - name: RETELL_API_KEY
        source: "Retell dashboard → Settings → API Key"
      - name: RETELL_WEBHOOK_SECRET
        source: "Retell dashboard → Agent → Webhook Secret"
      - name: SMTP_HOST
        source: "Your SMTP provider (e.g. smtp.gmail.com)"
      - name: SMTP_PORT
        source: "587 for TLS"
      - name: SMTP_USER
        source: "SMTP username / Gmail address"
      - name: SMTP_PASSWORD
        source: "SMTP password or Gmail App Password"
      - name: SMTP_FROM_ADDRESS
        source: "From address for sent emails"
    dashboard_config:
      - task: "Add Railway PostgreSQL plugin to the service and copy DATABASE_URL"
        location: "Railway dashboard → your service → + New → Database → PostgreSQL"
      - task: "Enable pgvector extension"
        location: "Railway PostgreSQL service → Connect → run: CREATE EXTENSION IF NOT EXISTS vector;"
      - task: "Set all env vars listed above in the Railway service Variables tab"
        location: "Railway dashboard → your service → Variables"
      - task: "Update Retell webhook URL to https://<railway-domain>/webhook"
        location: "Retell dashboard → Agent → Webhook URL"
---

<objective>
Deploy the FastAPI voice-agent backend to Railway with a public HTTPS URL so Retell can reach the /webhook endpoint from the internet.

Purpose: Make the system operational end-to-end so real phone calls can be handled.
Output: Running Railway deployment accessible at a public HTTPS URL, with Dockerfile and railway.toml committed.
</objective>

<execution_context>
@C:/Users/Russell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Russell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@voice-agent/app/main.py
@voice-agent/app/config.py
@voice-agent/requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Dockerfile and railway.toml</name>
  <files>voice-agent/Dockerfile
voice-agent/railway.toml
voice-agent/.env.example</files>
  <action>
**Dockerfile** (voice-agent/Dockerfile):

```dockerfile
FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 8000

CMD ["sh", "-c", "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"]
```

Key points:
- Use python:3.12-slim to match the dev environment.
- The CMD uses ${PORT:-8000} so Railway's injected $PORT env var is honored.
- No .env file is copied — all secrets injected as Railway env vars at runtime.

**railway.toml** (voice-agent/railway.toml):

```toml
[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
```

Railway uses this file to detect the Dockerfile location and set the health check.

**Update voice-agent/.env.example** to include all env vars the app requires (one per line, values are placeholder descriptions):

```
DATABASE_URL=postgresql+asyncpg://user:password@host:5432/dbname
ENCRYPTION_KEY=your-fernet-key-here
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
RETELL_API_KEY=your-retell-api-key
RETELL_WEBHOOK_SECRET=your-retell-webhook-secret
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM_ADDRESS=your-email@gmail.com
```

Note: .env.example must be explicitly added with `git add -f voice-agent/.env.example` because the root .gitignore has a `.env.*` wildcard (established decision from Phase 01).
  </action>
  <verify>ls E:/Antigravity/AgentTeam/voice-agent/Dockerfile && ls E:/Antigravity/AgentTeam/voice-agent/railway.toml && grep -c "=" E:/Antigravity/AgentTeam/voice-agent/.env.example</verify>
  <done>Dockerfile and railway.toml exist; .env.example has 13 lines with env vars</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Railway deployment is live and reachable</name>
  <action>
Human-only step: Deploy to Railway and confirm the public HTTPS URL is live.

Steps Russell must complete:
1. Create a Railway project and connect the GitHub repo (or push manually via Railway CLI: `railway up`)
2. Add a PostgreSQL plugin: Railway dashboard → your service → + New → Database → PostgreSQL
3. Enable pgvector: connect to the Railway PostgreSQL DB and run `CREATE EXTENSION IF NOT EXISTS vector;`
4. Set all env vars from voice-agent/.env.example in the Railway Variables tab (13 vars total)
5. Trigger a deploy — Railway auto-deploys on git push to main, or click "Deploy" in dashboard
6. Once the deploy shows green, copy the Railway-generated domain (e.g. https://voice-agent-production.up.railway.app)
7. Update the Retell Agent webhook URL to: https://your-railway-domain/webhook
  </action>
  <what-built>
    Task 1 produced:
    - voice-agent/Dockerfile — builds the FastAPI app container
    - voice-agent/railway.toml — tells Railway to use the Dockerfile and health check /health
    - voice-agent/.env.example — full list of required env vars
  </what-built>
  <how-to-verify>
    After completing Railway setup and deploy:

    1. Health check (no auth needed):
       curl https://your-railway-domain/health
       Expected: HTTP 200 with JSON response

    2. Webhook reachability (Retell signs this — just check it's reachable):
       curl -X POST https://your-railway-domain/webhook -H "Content-Type: application/json" -d '{}'
       Expected: HTTP 401 (signature missing — proves the endpoint is reachable and auth middleware is working)

    3. In Retell dashboard, confirm webhook URL is saved and the agent shows "Connected" status.
  </how-to-verify>
  <verify>curl https://your-railway-domain/health (replace with actual domain)</verify>
  <done>GET /health returns 200; POST /webhook returns 401; Retell agent webhook URL updated to Railway domain</done>
  <resume-signal>Type "deployed" once /health returns 200 from the Railway URL, or describe any issues encountered</resume-signal>
</task>

</tasks>

<verification>
Railway deployment verified when:
- curl https://railway-domain/health returns 200
- curl -X POST https://railway-domain/webhook returns 401 (endpoint reachable, auth working)
- Retell webhook URL updated to Railway domain
</verification>

<success_criteria>
- voice-agent/Dockerfile exists and builds cleanly
- voice-agent/railway.toml exists with healthcheckPath = "/health"
- voice-agent/.env.example lists all 13 required env vars
- Backend accessible at a public Railway HTTPS URL
- GET /health returns 200 from that URL
- POST /webhook returns 401 (reachable, signature required) from that URL
- Retell agent webhook URL updated to Railway domain
</success_criteria>

<output>
After completion, create `.planning/phases/05-admin-and-deployment/05-02-SUMMARY.md`
</output>
